import 'package:flutter/material.dart';
import 'package:flutter_hangouts/src/ui/homepage.dart';

void main() => runApp(MaterialApp(
      home: HomePage(),
      theme: ThemeData(brightness: Brightness.dark, fontFamily: 'Oswald'),
    ));
